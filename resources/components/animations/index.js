/**
 * Animation Component
 * 
 * Shows a select field for animations in the sidebar for every block
 * and adds a data-aos attribute to the save content of the block
 */

import './attributes.js';
import './inspector.js';
